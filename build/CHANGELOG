# CHANGELOG

## [Unreleased]

### Changed
- Merged `gateway` parameters into `backend` parameters: `backend.port` (default value), `backend.ipName`, `backend.hostname`
- Moved `backend.traceRequests` parameter to `backend.dumpRequests` to adapt to the implementation
- Moved ingress configuration from `http-gateway.libsonnet` to `core-service.libsonnet`

### Removed
- Removed `http-gateway.libsonnet` file and `gateway` deployment

