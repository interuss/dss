#!/usr/bin/env sh

# This script is intended to be called from within a Docker container running
# the interuss/monitoring image to determine the health status of the container.
# Certain monitoring tools may override this script upon startup.

exit 0
